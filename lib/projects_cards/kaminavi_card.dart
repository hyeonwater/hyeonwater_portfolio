import 'package:flutter/material.dart';

class KaminaviCard extends StatelessWidget {
  const KaminaviCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Dialog(
        child: Text('234'));
  }
}
