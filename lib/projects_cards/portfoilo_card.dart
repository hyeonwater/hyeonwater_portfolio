import 'package:flutter/material.dart';

class PortFoilioCard extends StatelessWidget {
  const PortFoilioCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('PoftFoilo');
  }
}
