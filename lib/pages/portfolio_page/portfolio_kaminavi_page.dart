import 'package:flutter/material.dart';

class PortfolioKaminavi extends StatelessWidget {
  const PortfolioKaminavi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Kaminavi');
  }
}
